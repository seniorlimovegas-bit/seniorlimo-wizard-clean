// pages/mwai-config.js
// NOTE: Anything inside /pages must export a default React component,
// otherwise Next.js build will fail.

export const MWAI_CONFIG = {
  appName: "SeniorLimo Mr. Wizard",
  version: "1.0.0",
};

export default function MWAIConfigPage() {
  // This page intentionally renders nothing.
  // We keep it only to satisfy Next.js build rules.
  return null;
}
